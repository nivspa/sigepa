# Copie para o servidor como deploy/shepherd.env (NÃO commitar).
# No Portainer: Stack → Environment variables → carregar este arquivo
# ou colar manualmente.

DOCKERHUB_USER=nivspa
DOCKERHUB_TOKEN=cole_aqui_o_token_do_docker_hub
